---
title: "How monitoring works"
description: "Connect a site once; chifu keeps scanning it and surfaces anything new."
---

Monitoring is the heart of chifu. You don't run scans by hand to stay safe: you
connect a [verified domain](/domains) and chifu re-checks it **automatically on a
schedule**, so a new CVE or a regression after a deploy gets caught on its own,
not whenever you remember to look.

Each check points chifu's engine at your **live** site and runs it through a
battery of probes. Nothing to install, no code changes.

## What chifu checks every run

<CardGroup cols={2}>
  <Card title="Known vulnerabilities" icon="shield-halved">
    Detects your framework and dependency versions and matches them against
    published CVEs. This is the core of "tell me when something new affects my
    stack."
  </Card>
  <Card title="Security headers" icon="lock">
    CSP, `X-Frame-Options`, HSTS, and friends.
  </Card>
  <Card title="TLS & certificates" icon="certificate">
    Certificate validity, protocol version, cipher strength.
  </Card>
  <Card title="Exposure" icon="eye">
    Leaked secrets, server banners, and info that shouldn't be public.
  </Card>
</CardGroup>

## Automatic vs. on-demand

- **Automatic (the default).** chifu [schedules scans for you](/schedule) at
  load-balanced times and re-runs them on a cadence that matches your plan. This
  is what makes it *monitoring* and not a one-off tool.
- **On-demand.** Want a result right now, say just after a deploy? Open
  **Scans**, click **Run scan now**, pick a verified domain from the dropdown,
  and click **Scan**.

<Note>
  The scheduling panel on the **Scans** page is just a quick explainer. There's
  no manual date or time picker. Your recurring scans are managed for you on the
  [Schedule](/schedule) page.
</Note>

<Note>
  Every scan, automatic or manual, counts against your monthly quota (3 on Free,
  30 on Pro, 120 on Max). See [Plans](/plans).
</Note>

## Live status

Scans move through `Queued → Running → Completed` and the dashboard updates live.
Most static sites finish in seconds; larger sites take a few minutes. You can
leave the page, it keeps running. The **Scan history** table shows each run's
status, target, start time, duration, and findings count.

| Status | Meaning |
| --- | --- |
| **Queued** | Accepted, waiting for a worker. |
| **Running** | Actively probing your site. |
| **Completed** | Done, the full report is ready. |
| **Failed** | Something went wrong (for example, unreachable). The report says why. |

## What a scan report contains

Click any scan to open its detail page:

<CardGroup cols={2}>
  <Card title="Risk score" icon="gauge" href="/risk-score">
    A 0 to 99 gauge with a color band, higher is safer.
  </Card>
  <Card title="Score breakdown" icon="chart-simple" href="/risk-score">
    Five components (Vulnerabilities, TLS, Security headers, Exposure, Defensive
    posture), each scored 0 to 100, with deltas when you re-scan.
  </Card>
  <Card title="Tech stack" icon="layer-group">
    What chifu detected powering your site, with versions.
  </Card>
  <Card title="Known vulnerabilities" icon="shield-halved">
    A CVE table matched to your detected versions, each linking to NVD.
  </Card>
  <Card title="Other findings" icon="bug" href="/findings">
    Missing headers, exposed banners, and everything else, with fixes.
  </Card>
</CardGroup>

## Hear about it automatically

The point of monitoring is not having to check. Wire up [alerts](/alerts) and
chifu pings you on Discord the moment a scan finds something serious, so the
dashboard is where you go to *fix*, not to *discover*.
